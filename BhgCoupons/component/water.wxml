 <view class='water-mark-mask'>
    <view class='row' wx:for="{{rows}}" wx:key="index">
      <view class='col' wx:for="{{cols}}" wx:key="index" style="color:{{color}};font-size:88rpx;">{{text}}</view>
    </view>
  </view>
